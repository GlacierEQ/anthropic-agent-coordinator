name: Agent Coordinator Verification

on:
  push:
    branches: [master, "wave-1/**"]
  pull_request:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: coordinator-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Python ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    uses: GlacierEQ/public-actions-runner-host/.github/workflows/reusable-quick-ci.yml@main
    with:
      repo_name: ${{ github.event.repository.name }}-py${{ matrix.python-version }}
      language: python
      strict: true
      verify_script: scripts/ci/verify.sh
      python_version: ${{ matrix.python-version }}
      upload_artifacts: true
